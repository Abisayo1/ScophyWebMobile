<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CloudSpace_Scophy" parent="Theme.Material3.DayNight">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CloudSpace_Scophy" parent="Theme.Material3.DayNight" />
    <style name="Theme.App.SplashScreen" parent="Theme.Material3.DayNight">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo_s</item>
        <item name="postSplashScreenTheme">@style/Base.Theme.CloudSpace_Scophy</item>

    </style>
</resources>